# 一、总体（摘要）

- 主机：Windows 11 专业版（Build 26200）
- WSL：Ubuntu-24.04（WSL2, 默认）
- 开发位置（Windows）：`D:\workspaceAI\pdis`（注意：在 WSL 中访问为 `/mnt/d/workspaceAI/pdis`）
- GPU：NVIDIA GeForce RTX 5070 Ti（16,303 MiB VRAM） — 驱动 581.80 / CUDA 13.0（已检测）。
- 内存：约 32 GB（物理）
- 可用硬盘：约 1.23 TB（你列出的盘很多，项目可放 D 盘）

------

# 二、必备环境与版本（必须确认/已就绪）

（把你的现状直接列出并补充推荐最小版本）

**操作系统 / 基础工具**

- Windows 11 Pro 10.0.26200（已安装）

- PowerShell（管理员权限） — 用于 WSL / Docker 操作

- 推荐验证命令（在 PowerShell 运行）：

  ```
  winver
  wsl --status
  wsl -l -v
  docker version
  docker compose version
  ```

**WSL / Linux**

- WSL2 上的 Ubuntu 24.04（已就绪）

- 验证（在 WSL）：

  ```
  lsb_release -a
  uname -r
  docker --version
  docker compose version
  git --version
  python3 --version
  pip3 --version
  node --version
  npm --version
  npx --version
  nvidia-smi
  ```

**关键已安装/检测到版本（你现状）**

- Python 3.12.3（WSL）
- Node.js v22.22.0、npm 10.9.4
- Docker 29.2.0、Docker Compose v5.0.2
- Git 2.43.0
- nvidia driver 581.80 / CUDA 13.0（`nvidia-smi` 已显示）

------

# 三、硬件（具体值）

- CPU：Intel Core i7-14700KF（20 cores, ~3.4GHz）
- RAM：32,539 MB（约 32 GB）
- GPU：NVIDIA GeForce RTX 5070 Ti — 16303 MiB VRAM（可用于本地模型推理）
- 磁盘：列出盘（可按需要分区）：
  - ZHITAI TiPlus7100 2TB
  - Samsung SSD 970 EVO 500GB
  - ST2000DM008-2TB
  - WDC WD40EZRZ-4TB
- 页面文件：`D:\pagefile.sys`（存在）

------

# 四、工作目录映射（必须使用）

- Windows 路径（你给的）：`D:\workspaceAI\pdis` ← 推荐用于代码/项目根
- WSL 对应路径：`/mnt/d/workspaceAI/pdis`

> 请在 WSL 内部始终在 `/home/<user>/…` 或 `/mnt/d/...` 下工作，避免直接在 Windows 文件系统上大量 IO（保留必要时使用）。

------

# 五、软件与服务清单（将被用于开发/运行/部署）

我把它们分成“必须 / 推荐 / 可选”三组。

## 必须（MVP 运行所需）

- **Docker Desktop**（Windows） — 启用 WSL2 后端、WSL Integration（Ubuntu-24.04）
- **Docker Compose**（或 `docker compose` 插件）
- **WSL2 Ubuntu 24.04**（已就绪）
- **Ollama**（Windows 本地模型管理、运行时；用于本地 LLM 推理） — 注意：若 Ollama 在 Windows 运行，需开放网络访问（`OLLAMA_HOST` 环境变量）。
- **PostgreSQL with pgvector**（在 docker compose 中部署或云）
- **向量数据库（选一）**：Qdrant / Milvus / Chroma（PoC 用 Qdrant 可快速上手；生产可评估 Milvus）
- **FastAPI**（后端框架，Python） + **uvicorn**（ASGI server）
- **Python**（3.10+，你已 3.12 可用）
- **Node.js**（v18+ 推荐或你现有 v22） + **Vite**（构建工具）
- **Vue 3**（前端框架） + Element Plus（UI 组件库）

## 推荐（强烈建议用于工程化）

- **Redis**（缓存 / 任务队列/ Celery / RQ 后端）
- **Celery** 或 **RQ**（异步任务队列，用于 embedding / LLM 调用）
- **MinIO**（S3-compatible 本地存储，用于证据/附件）
- **HashiCorp Vault**（或本地 secrets manager） — 管理 API keys / DB 密钥。
- **Prometheus + Grafana**（监控）
- **ELK/EFK（Elastic / Fluentd / Kibana）或 Loki**（日志/审计）
- **pgcli / psql**（方便 DB 操作）
- **psycopg2-binary / sqlalchemy / alembic**（Python DB stack）
- **sentence-transformers**（用于生成本地 embeddings，如果不想使用云 embeddings）
- **torch / huggingface/transformers**（如果要在本地跑 embedding 或小模型）

## 可选（按需）

- **OpenAI API key**（云端 fallback / 高质量推理）
- **Alibaba Cloud LLM SDK**（若要试阿里云模型）
- **CI/CD 工具**（GitHub Actions / GitLab CI）
- **Kubernetes**（生产级扩容时）

------

# 六、模型清单（你列的 + 推荐）

> 我保留并标准化你列的模型名与来源（已验证公开信息）。若要本地运行，请确认许可与运行资源（VRAM）。

**你已列的 / 我建议纳入的模型**

- `Qwen3-8B`（又写作 `qwen3:8b`） — Alibaba Qwen3 系列的 8B 模型（可用于本地/私有部署或云）。
- `TranslateGemma:12B`（也称 `translategemma:12b`）— Google TranslateGemma 系列（4B/12B/27B），专注翻译任务，可本地部署 / Ollama 库中有条目。
- **本地通用 LLM（通过 Ollama / Llama cpp 等）**，用于敏感数据本地处理（例如 LLaMA-family、Mistral、Qwen 等）
- **Cloud LLM（备援）**：OpenAI GPT-4 / GPT-4o 等（按预算/合规启用）
- **Embedding 模型**（本地优先）：sentence-transformers (e.g. `all-MiniLM-L6-v2`) 或者本地小型 OpenAI-like embedder；若用云，可用 OpenAI embeddings（注意 PII）。

> 备注：TranslateGemma 属于 Google 发布的 TranslateGemma 系列（4B/12B/27B），适合翻译场景；Qwen3-8B 是 Qwen3 系列的 8B 实现。两者都是公开可用模型（确认来源后即可下载/在 Ollama / HuggingFace 使用）。

------

# 七、开发工具（IDE / 扩展 / 辅助）

- **VS Code**（主 IDE）
  - 推荐扩展：Remote - WSL、Python (Microsoft)、Pylance、Docker、ESLint、Vetur/Volar (Vue 3)、SQLTools + SQLTools PostgreSQL Driver、GitLens、Markdown All in One、EditorConfig
- **psql / pgcli**（Postgres 管理）
- **Postman / HTTPie / curl**（API 调试）
- **Chrome/Edge + Vue devtools**（前端调试）

------

# 八、Docker / 容器镜像（PoC 最低集合）

- `ankane/pgvector:latest` 或 `postgres:15 + pgvector extension`（Postgres + pgvector）
- `qdrant/qdrant:latest`（Qdrant 向量库） — 或 `milvusdb/milvus`（Milvus）
- `redis:7`（可选：任务队列）
- `python:3.12-slim`（后端容器镜像）
- `node:18-alpine`（前端构建）
- `minio/minio`（可选：本地 S3）
- （本地 LLM 可不容器化，使用 Ollama Desktop / Windows 服务）

------

# 九、网络 / 端口 / 环境变量（默认端口）

- Postgres：`5432`
- Qdrant：`6333`
- Redis：`6379`
- FastAPI（开发）：`8000`（uvicorn）
- Ollama（默认 HTTP API）：`11434`（若通过 host.docker.internal 访问则注意防火墙）
- OLLAMA_HOST：建议在 Windows 用户环境变量中 `setx OLLAMA_HOST 0.0.0.0`（使 WSL/container 能访问 Ollama），但在公开网络环境应更严格绑定。

> 防火墙规则：确保 Docker Desktop 与 Ollama 的端口在 Windows 防火墙中允许本机访问（仅本机/内部网络）。

------

# 十、数据库 & schema 要点（最小）

- 表：`person_profile`, `event`, `analysis_result`, `audit_log`（见我之前给出的 SQL 架构）
- 强制要求：`analysis_result.json_output` 为 JSONB；保存 `model_name`, `model_version`, `prompt_template`, `retrieved_docs_ids` 与 `sensitivity_flag`（用于审计）
- 建议：为敏感字段启用字段级加密或使用 Vault 管理加密密钥

------

# 十一、安全 / 合规清单（必须执行）

- 标记并阻止 `sensitivity==true` 数据上云（包括 embeddings）
- Secrets 管理（Vault）→ 不在代码或 .env 明文保存 API keys
- 审计日志 —— 不可篡改（append-only）
- 备份：Postgres daily snapshot + Qdrant snapshot（或导出向量与 metadata）
- 权限：最小化服务账户权限，UI 导出敏感数据需二次确认

------

# 十二、常用验证/排错命令（把你历史命令整理成一套）

**Windows PowerShell**

```
winver
wsl --status
wsl -l -v
docker version
docker compose version
```

**WSL / Ubuntu**

```
lsb_release -a
uname -r
docker --version
docker compose version
git --version
python3 --version
pip3 --version
node --version
npm --version
npx --version
nvidia-smi
curl --connect-timeout 5 http://host.docker.internal:11434/api/tags  # 测试 Ollama 可达性
```

------

# 十三、校正与补充（基于你提供资料我做了哪些修正）

1. **向量库项**：“embedding” 不是库名 —— 我把它明确为 Qdrant / Milvus / Chroma 等可选项，并补充 pgvector（Postgres 扩展）作为另一种选择。
2. **前端细化**：`vue3 (框架:)` 补全为 `Vue 3 + Vite + Element Plus`（或 Ant Design Vue），并推荐 Node v18+/22（你已有 v22）。
3. **后端细化**：`python (框架)` 补全为 `Python + FastAPI + uvicorn + Pydantic + SQLAlchemy`（推荐栈）。
4. **模型名称确认**：`qwen3:8b` 对应 `Qwen3-8B`；`translategemma:12b` 对应 `TranslateGemma 12B`（Google）；两者均可本地或通过 Ollama/HuggingFace 使用。已给出处。
5. **补充组件**：Redis（队列）、MinIO（附件）、Vault（secret）等都被加入到推荐清单中。
6. **Docker 问题提示**：从你日志看 `docker` 客户端有一次报错（找不到 Docker daemon pipe）。结论：需要确保 Docker Desktop 正在运行（Daemon 未运行会导致 `docker` 命令失败）。

------

# 十四、立即优先要做的 3 件事（短期动作）

1. **启动并验证 Docker Desktop**（Windows）：打开 Docker Desktop → 确认 WSL 集成已启用；在 PowerShell 运行 `docker version`，若报错，重启 Docker Desktop 并检查 Windows 防火墙/WSL。

2. **设置 OLLAMA_HOST 环境变量（如果你用 Ollama Desktop 在 Windows）**：

   - 在管理员 PowerShell 运行：

     ```
     setx OLLAMA_HOST 0.0.0.0
     ```

     然后彻底退出 Ollama（托盘退出）并重启 Ollama。用 `curl` 从 WSL 验证：

     ```
     curl --connect-timeout 5 http://host.docker.internal:11434/api/tags
     ```

3. **在 WSL 中启动 PoC docker-compose（Postgres + pgvector + Qdrant）**，并验证 `psql` / `curl http://localhost:6333` 可达（我可把你现成的 compose 做成文件并发给你）。

------

# 十五、待你确认（我将按默认值继续）

- 是否以 **Qdrant** 作为 PoC 向量库？（默认：是）
- Cloud fallback：默认保留 **OpenAI**（若你想用阿里云/其他，请告知，我会加入 SDK 与配套配置）
- 模型下载/许可：默认我们**不自动下载**大型模型，先通过 Ollama 管理你已列出的模型（Qwen3-8B / TranslateGemma:12B）。若你希望我提供 `ollama pull` / huggingface 下载示例，我会直接给出命令与资源路径。

（我将按上面默认值继续，除非你明确想替换）

------

# 十六、我可以马上为你做（任选其一 — 我会在本回复后立即产出）

1. 把上述必需项生成为**机器可执行的清单（脚本 + docker-compose.yml）**，可在 WSL 直接执行并搭起 PoC 环境。
2. 生成**环境校验脚本**（PowerShell + WSL 脚本），一次性验证 Docker、nvidia-smi、Ollama 可达性、Postgres/Qdrant 可达。
3. 生成**完整的依赖列表（requirements.txt + package.json）和初始 FastAPI 项目骨架**（包含 Pydantic models、数据库连接、基本 CRUD）。