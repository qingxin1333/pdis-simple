# 一、系统环境（System Environment）

## 1、操作系统（Host）

### 1.1 Windows 主机

- 操作系统：Microsoft Windows 11 专业版
- 版本号：10.0.26200
- Build：26200
- 架构：x64-based PC
- 时区：(UTC+08:00) 北京时间
- 虚拟化支持：已开启（Hyper-V / VBS / WSL2 可用）

### 1.2 WSL 子系统

- WSL 版本：WSL2
- 默认发行版：Ubuntu-24.04
- Linux 内核：6.6.87.2-microsoft-standard-WSL2
- 发行版版本：Ubuntu 24.04.3 LTS (noble)

------

# 二、硬件配置（Hardware）

## 1、处理器

- CPU 型号：Intel(R) Core(TM) i7-14700KF
- 核心数：20
- 主频：~3.40 GHz

## 2、内存

- 物理内存总量：32,539 MB（≈ 32 GB）
- 当前可用内存：≈ 21–23 GB（动态）

## 3、显卡

- GPU 型号：NVIDIA GeForce RTX 5070 Ti
- 显存：16,303 MiB
- 驱动版本：581.80
- CUDA 版本：13.0
- WSL GPU 透传：✅ 已验证（`nvidia-smi` 正常）

## 4、存储设备

- NVMe：ZHITAI TiPlus7100 2TB
- SSD：Samsung 970 EVO 500GB
- HDD：Seagate 2TB
- HDD：Western Digital 4TB
- 项目可用空间：≈ 1.23 TB（有效可用）

------

# 三、工作目录与路径规范（Workspace）

## 1、Windows 侧目录

- 项目根目录：

  ```
  D:\workspaceAI\pdis
  ```

## 2、WSL 映射路径

- 对应 Linux 路径：

  ```
  /mnt/d/workspaceAI/pdis
  ```

> 说明：
>
> - **代码开发主要在 WSL 内执行**
> - Windows 路径仅作为宿主文件系统映射
> - Docker / Python / Node 均在 WSL 环境运行

------

# 四、技术选型（Technology Stack）

## 1、前端技术栈

### 1.1 框架

- Vue 3
- 构建工具：Vite

### 1.2 UI 组件

- Element Plus（管理系统 / 表单 / 表格）

### 1.3 前端运行环境

- Node.js：v22.22.0
- npm：10.9.4
- npx：10.9.4

------

## 2、后端技术栈

### 2.1 语言与运行时

- Python：3.12.3

### 2.2 Web 框架

- FastAPI
- ASGI Server：Uvicorn

### 2.3 核心依赖（规划中）

- Pydantic（数据校验 / Schema）
- SQLAlchemy（ORM）
- psycopg2-binary（PostgreSQL 驱动）

------

## 3、数据库

### 3.1 关系型数据库

- PostgreSQL 15+
- 存储内容：
  - 人物档案（结构化事实）
  - 事件记录
  - AI 分析结果（JSONB）
  - 审计日志

### 3.2 向量扩展

- pgvector（PostgreSQL 扩展，用于向量字段）

------

## 4、向量数据库（Vector Store）

### 4.1 类型

- Qdrant（PoC / 本地优先）
- （可选）Milvus（后期扩展）

### 4.2 用途

- 事件文本 embedding 存储
- 人物 / 行为 / 场景相似度检索
- RAG（检索增强生成）输入来源

------

# 五、开发工具（Development Tools）

## 1、IDE

- Visual Studio Code

## 2、VS Code 插件

### 2.1 必装插件

- Remote - WSL
- Python（Microsoft）
- Docker

### 2.2 数据库相关

- SQLTools
- SQLTools PostgreSQL Driver

------

# 六、基础软件与服务（Software & Services）

## 1、容器与虚拟化

- Docker Desktop（Windows）
- Docker Engine（WSL2 后端）
- Docker Compose v5.0.2

## 2、本地 AI 服务

- Ollama（Windows 本地运行）
- API 访问端口：11434
- 网络模式：WSL / Docker 可访问（`OLLAMA_HOST=0.0.0.0`）

------

# 七、AI 模型清单（Models）

## 1、本地模型（Ollama）

### 1.1 通用大模型

- Qwen3-8B
  - 用途：人物分析、决策推理、结构化输出

### 1.2 专用模型

- TranslateGemma 12B
  - 用途：翻译、多语言文本处理

## 2、云端模型（可选）

### 2.1 云 LLM

- 阿里云 Cloud LLM（OpenAI 兼容接口）
- 使用场景：
  - 非敏感数据
  - 高质量复杂推理
  - Token 受限时的补充方案

------

# 八、部署方式（Deployment）

## 1、本地部署模式

- Docker Compose
- 服务：
  - PostgreSQL + pgvector
  - Qdrant
  - 后端 FastAPI
  - （可选）Redis / MinIO

## 2、系统服务形态

- Windows 服务：
  - Docker Desktop
  - Ollama
- WSL 服务：
  - Docker 容器
  - Python 后端服务

------

# 九、当前环境可用性确认（Ready Check）

## 1、基础能力

- Windows 11 Pro：✅
- WSL2 Ubuntu 24.04：✅
- Docker & Compose：✅
- Python / Node / Git：✅
- GPU / CUDA：✅
- Ollama（Windows）：✅

## 2、是否可进入开发阶段

- 结论：**可以立即进入开发阶段（无阻断项）**